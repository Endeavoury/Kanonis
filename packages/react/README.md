# @endeavoury/kanonis-react

Thin `@lit/react` adapters for the Web Components in `@endeavoury/kanonis`. This package adds typed React props and custom events; it contains no separate component implementation.
