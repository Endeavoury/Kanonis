name: kanonis

services:
  storybook:
    image: finance-inzicht-design-system:local
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${DESIGN_SYSTEM_PORT:-6006}:8080"
    restart: unless-stopped
